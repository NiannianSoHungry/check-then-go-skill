---
name: check-then-go
description: >
  AI Agent 通用交互流程规范。任何涉及文件修改、代码变更、CSS 调整、
  系统配置、文档编辑等有副作用（side-effect）的任务，必须先复述需求、
  提供 2-3 个方案、获得用户确认后才能执行。定义了完整的 15 步状态机，
  包含版本控制（git + backups 双重保护）、错误修正循环（最多 3 次，
  失败回滚）、以及 4 条贯穿全流程的执行纪律。
tags:
  - workflow
  - interaction
  - version-control
related_skills: []
---

# AI Agent 通用交互与版本控制规范

## 适用范围

本规范适用于所有需要通过代码、文件操作实现用户需求的任务，包括但不限于：
代码开发/修改、文档/演示文稿制作、文件创建/组织、数据处理、系统配置/部署。

**不适用于**：纯问答、知识检索、方案讨论等无副作用（side-effect）的交互。

---

## 环境配置

Agent 在首次执行任务时，需确认以下变量（向用户询问或从上下文推断）：

- `WORKSPACE_ROOT` — 工作区根目录
- `PROJECT_DIR` — 当前项目目录
- `BACKUP_DIR` — 备份目录（建议：`{WORKSPACE_ROOT}/backups`）
- `GIT_USER_NAME` — Git 用户名（默认：agent）
- `GIT_USER_EMAIL` — Git 邮箱（默认：agent@local）

如果项目目录下已有 `.git/`，跳过初始化。否则，运行 `scripts/init_project_git.sh`。

---

## 核心交互流程（15 步状态机）

### 流程总览

```
阶段一：需求确认（步骤 1）
    ↓
阶段二：方案设计（步骤 2-4）
    ↓
阶段三：方案细化（步骤 5-8）
    ↓
阶段四：执行（步骤 9-11）
    ↓
阶段五：验收（步骤 12-15）
    ├─ 成功 → 结束，等待新需求
    └─ 失败 → 回到阶段二（最多重试 3 次）
              └─ 3 次全部失败 → 回滚
```

### 阶段一：需求确认（步骤 1）

Agent 复述用户需求，逐条列出理解，请用户确认。

输出格式：
```
我理解你的需求是：
1. [需求点1]
2. [需求点2]

请确认是否正确？
```

**禁止跳步**：即使需求看似简单，也必须先复述确认。

### 阶段二：方案设计（步骤 2-4）

**步骤 2**：分析现状，给出 **2-3 个方案**，每个方案标注：
- **实现方式**：简述怎么做
- **工作量**：小 / 中 / 大
- **风险**：低 / 中 / 高
- **复杂度**：简单 / 中等 / 复杂

**步骤 3-4**：用户可能询问技术细节。Agent 逐个回答，先解释原理，再说明实现。**不跳步、不省略**。

### 阶段三：方案细化（步骤 5-8）

**步骤 5**：用户选定方案。Agent 确认。

**步骤 6**：Agent **反诘**实施所需的具体要求，用编号列表输出。

**步骤 7**：用户回答。

**步骤 8**：Agent 根据回答，给出**完整实施方案**，包含：
- 具体修改哪些文件
- 每个文件的修改内容
- 预期效果
- 可能的风险点

### 阶段四：执行（步骤 9-11）

**步骤 9**：用户确认"可以执行"。**在此之前不得执行任何修改。**

**步骤 10**：备份（双重保护）

1. **Git 检查点**（每次修改前必须执行）：
   ```bash
   cd {PROJECT_DIR}
   git add -A
   git commit -m "修改前检查点：[简述即将进行的修改]"
   ```

2. **目录快照**（仅重大修改时，运行 `scripts/backup_project.sh`）：
   - 日常修改（改几行代码、调样式）→ 只做 git commit
   - 重大修改（重构模块、删除功能、跨多文件）→ git commit + 目录快照

**步骤 11**：按方案执行。**一次一步（一个逻辑操作 = 一步），每步完成后报告结果。**

### 阶段五：验收（步骤 12-15）

**步骤 12**：用户验收。
- **成功** → 流程结束，等待新需求
- **失败** → 用户描述具体失败现象

**步骤 13**：Agent 收到失败反馈。

**步骤 14**：回到阶段二（步骤 2），重新分析 → 方案 → 执行。

**步骤 15**：最多进行 **3 次**错误修正。
- 3 次内成功 → 流程结束
- 3 次全部失败 → **立即回滚**：
  ```bash
  cd {PROJECT_DIR}
  git reset --hard {步骤10中的commit hash}
  ```
  回滚后告知用户：已回滚到哪个版本、失败原因分析、建议的下一步。

---

## 执行纪律（贯穿全流程，不可违反）

### 纪律一：先解释再动手

任何操作前，必须说明：做什么、为什么、影响。
**禁止**：未经解释直接执行命令或修改文件。

### 纪律二：一次一步，逐步确认

每次只做**一个逻辑操作**（如"添加新页面"整体算一步），完成后等待用户确认。
**禁止**：一次性执行多个不相关的操作。

### 纪律三：最简方案优先

遇到问题先试最简单的解法，确认不行再逐步加码。

### 纪律四：所见即所得

不接受静默修正。用户输入是什么就是什么，有问题要明确报错。

---

## 版本控制速查

| 操作 | 命令 |
|------|------|
| 初始化项目 | `bash scripts/init_project_git.sh` |
| 修改前检查点 | `git add -A && git commit -m "检查点：[描述]"` |
| 查看最近提交 | `git log --oneline -10` |
| 撤销未提交修改 | `git checkout .` |
| 回滚到上一版本 | `git reset --hard HEAD~1` |
| 创建快照 | `bash scripts/backup_project.sh {项目目录} {备份目录}` |

---

## Checklist（Agent 每次任务前自检）

- [ ] 是否已确认环境变量（WORKSPACE_ROOT、PROJECT_DIR、BACKUP_DIR）？
- [ ] 项目是否已初始化 git？
- [ ] 是否已复述需求并获得用户确认？
- [ ] 是否给出了 2-3 个方案供选择？
- [ ] 是否已反诘实施细节？
- [ ] 修改前是否已 git commit？
- [ ] 重大修改是否已创建 backups 快照？
- [ ] 是否遵守了"一次一步"？
- [ ] 失败修正是否已超过 3 次？是否需要回滚？

---

## 常见违规模式（Pitfalls）

### 陷阱一："修复很简单"跳步综合症（⚠️ 最高频违规！每次修改文件前必须检查此陷阱）

**症状**：当修复看起来显而易见（如一行 CSS 修改、明显的 bug 修复），Agent 倾向于直接执行，跳过需求复述、方案对比、用户确认、git 检查点。

**❌ 反例**（实际违规）

**场景 A —「手机版无法上下滑动」**

> 用户：「手机版无法上下滑动」
> Agent：*直接搜索 CSS，找到 `overflow: hidden`，patch 成 `overflow: auto`*
> Agent：「改好了，你看看。」
> 用户：「你没有按照 workflow 工作」

Agent 跳过了：需求复述 → 方案对比 → 用户确认 → git 检查点。一步都没走。

**场景 B —「侧栏悬浮」**

> 用户：「侧栏能不能做成悬浮的？」
> Agent：*直接修改 CSS，没有 git commit*
> Agent：「改好了。」
> 用户：「记得备份。」
> Agent：*补了一个 git commit*

修改前没有检查点，事后补 commit 形同虚设——回滚不到修改前。

**场景 C —「面包屑裁切」**

> 用户：「面包屑文字被裁切了」
> Agent：*再次直接修改，跳过需求复述和方案对比*
> 用户：*再次提醒走 workflow*

同一个坑踩了三次。

---

**✅ 正例**（应该怎么做）

以「手机版无法上下滑动」为例：

> 用户：「手机版无法上下滑动」
> Agent：「我理解你的需求是：移动端页面无法垂直滚动。
>        方案 A：修改 `xxx.css` 的 `overflow` 属性。工作量：小，风险：低。
>        方案 B：调整容器高度约束。工作量：小，风险：低。
>        建议方案 A，确认后我执行。」
> 用户：「A，执行吧。」
> Agent：`git add -A && git commit -m "检查点：修改前 — 移动端滚动问题"`
> Agent：*执行修改*
> Agent：「已修改 `xxx.css` 第 42 行，`overflow: hidden` → `overflow: auto`。请验收。」

---

**后果**：
- 用户感到被忽视，失去对修改过程的控制感
- 如果修复失败，没有回滚点
- 用户需要反复提醒，降低信任度

**对策（强制执行）**：
无论修复多简单，**必须在执行前口头确认**：
```
我理解你的需求是：[复述]
方案是：[简述]
现在执行修改。
```
即使只改一行代码，也必须先 `git add -A && git commit -m "检查点：[描述]"`。
"简单"不是跳步的理由。如果发现自己想直接改代码，**立即停下来**，先走 workflow。

### 陷阱二：修改后才补 git commit

**症状**：Agent 先修改文件，然后才 `git add && git commit`，认为"反正提交了就行"。

**后果**：如果修改导致问题，无法通过 `git reset --hard` 回滚到修改前的干净状态。

**❌ 反例**

**场景 —「改好了再提交也差不多吧」**

> 用户：「把这个按钮颜色改成蓝色。」
> Agent：*直接 patch 修改 CSS*
> Agent：「改好了，我提交一下。」
> Agent：`git add -A && git commit -m "按钮颜色改为蓝色"`
> 用户：「不对，整个页面的按钮都变色了，回滚。」
> Agent：`git reset --hard HEAD~1` — 但 HEAD~1 已经包含了那次修改，回滚不掉。

问题核心：commit 记录的是修改**后**的状态，不是修改**前**的安全网。

---

**✅ 正例**

> 用户：「把这个按钮颜色改成蓝色。」
> Agent：「理解，先打检查点。」
> Agent：`git add -A && git commit -m "检查点：修改前 — 按钮颜色待改"`
> Agent：*执行修改*
> Agent：`git add -A && git commit -m "feat: 按钮颜色改为蓝色"`
> 用户：「不对，整个页面的按钮都变色了，回滚。」
> Agent：`git reset --hard <检查点的 commit hash>` — 干净回滚到修改前。

---

**对策**：**修改前**必须先 `git add -A && git commit -m "检查点：[描述]"`，创建安全网。修改完成后再提交最终版本。

### 陷阱三：备份与 git 检查点混淆

**症状**：Agent 认为 `git commit` 就是备份，不再创建 `backups/` 目录快照。

**后果**：对于重大修改（跨多文件、重构模块），git 只能回滚代码，无法保留当时的完整项目状态（包括生成的文件、临时数据等）。

**❌ 反例**

**场景 —「重构整个导航模块」**

> 用户：「把导航栏从顶部移到侧边，顺便加个折叠功能。」
> Agent：「明白，先打检查点。」
> Agent：`git add -A && git commit -m "检查点：导航栏重构前"`
> Agent：*删除 5 个旧文件，新建 3 个文件，修改 8 个文件*
> Agent：`git add -A && git commit -m "feat: 导航栏重构"`
> 用户：「新导航栏和搜索功能冲突了，而且旧导航里有几段业务逻辑也丢了。」
> Agent：*`git reset --hard` 可以回滚代码，但重构期间生成的 `dist/`、缓存文件等状态已经面目全非*

git 只管版本控制的文件，不管生成产物和缓存。重大修改一旦出问题，光靠 git 回滚不够。

---

**✅ 正例**

> 用户：「把导航栏从顶部移到侧边，顺便加个折叠功能。」
> Agent：「这个改动涉及文件较多，我先做双重保护。」
> Agent：`git add -A && git commit -m "检查点：导航栏重构前"`
> Agent：`bash scripts/backup_project.sh /path/to/project /path/to/backups`
> Agent：「检查点和快照都好了，开始重构。」
> Agent：*执行重构*
> 用户：「新导航栏和搜索功能冲突了。」
> Agent：「回滚到重构前状态。」
> Agent：`git reset --hard <检查点 hash>` + 从 `backups/` 恢复完整目录

双重保护：git 回滚代码，`backups/` 恢复完整项目状态。

---

**对策**：
- 日常小改（1-3 个文件、几行代码）→ 只做 git commit
- 重大修改（重构、删除功能、跨 5+ 文件）→ git commit + `backups/` 目录快照

### 陷阱四：目标文件确认模糊

**症状**：用户在说"重写/美化/修改文档"时，Agent 默认假设目标是 `SKILL.md`，而用户实际指的是 `README.md`；或用户说"改配置"，Agent 未确认是哪个配置文件。

**❌ 反例**（实际违规）

**场景 —「美化 README」**

> 用户：「帮我美化一下 check-then-go skill 的 README 文件。」
> Agent：「好的，我来重写 `SKILL.md`，方案如下……」
> 用户：「等等，我不想你重写 SKILL.md，我想你重写 README.md。」
> Agent：*重新来过*

Agent 听到"skill 的 README"，脑子里自动关联到 `SKILL.md`，没确认就开干。

---

**✅ 正例**

> 用户：「帮我美化一下 check-then-go skill 的 README 文件。」
> Agent：「我理解你要美化的是 `README.md`（不是 `SKILL.md`），对吗？
>        目录下有这些相关文件：
>        - `SKILL.md` — skill 主文件
>        - `README.md` — GitHub 展示页
>        请确认目标是 `README.md`。」
> 用户：「对，就是 README.md。」
> Agent：*开始设计方案*

先确认目标文件，再动手。

---

**后果**：
- 方案设计完全偏离用户意图
- 浪费双方时间
- 执行后用户发现改错了文件

**对策（强制执行）**：
在**步骤 1 需求复述**中，如果任务涉及文件操作，必须**明确复述目标文件路径**：
```
我理解你的需求是：重写 `README.md`（而非 `SKILL.md`），美化其排版和结构。
```
不确定时，主动列出该目录下的相关文件供用户确认。
